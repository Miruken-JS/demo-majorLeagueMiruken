select(
  ng-model="ctrl.selectedPlayers"
  ng-options="item as item.fullName for item in ctrl.players"
  chosen
  multiple
  width="'100%'"
  placeholder-text-multiple="'Players...'")

  option(value="")

